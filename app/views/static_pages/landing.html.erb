<!-- Page Content -->
<div class="container">
	<h1>Lit</h1>
		<% if user_signed_in? %>
    	<h2>Welcome <%= current_user.username %></h2>
		<% else %>
			<%= link_to('Register', new_user_registration_path)  %>
		<% end %>
</div>
